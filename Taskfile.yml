# https://taskfile.dev

version: '3'

tasks:
  test:
    cmds:
      - go test -v ./...
  lint:
    cmds:
      - golangci-lint run ./...
  sqlc:
    cmds:
      - sqlc generate
  initdata:
    cmds:
      - go run ./tests/initdata.go
